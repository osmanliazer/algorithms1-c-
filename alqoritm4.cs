

int[] arr = { 1, 3, 4, 8, 5, 13, 21, 33, 54 };
int count = 0;
for (int i = 0; i < arr.Length; i++) 
{
    if (i > 0)
    {
        (arr[i] % 2 != 0 && (arr[i] % 3 == 0);
    }
    Console.WriteLine(count++);
}